<div class="blog-show-container">
  <div class="blog-content">
    <div class="blog_text">
      <div id="blog-header">
        <h1><%= @content.title %></h1>
      </div>
      <div class="d-flex username-info align-items-center justify-content-between mb-2">
        <div class="creator-info d-flex align-items-center">
          <%= cl_image_tag @content.user.profile_pic.key, class: "comment-img" %>
          <h4><%= @content.user.username %></h4>
        </div>
        <div class="fav-heart d-flex align-items-center justify-content-between">
          <!-- favourite heart -->
          <% if current_user&.favorited?(@content) %>
            <%= link_to unfavorite_content_path(@content) do %>
              <i id="blog-like" class="fas fa-heart"></i>
            <% end %>
          <% elsif current_user %>
            <%= link_to favorite_content_path(@content), id:"like-button", remote: true do %>
              <i id="blog-like" class="far fa-heart"></i>
            <% end %>
          <% end %>
          <p class="fv-counter"><%= @content.favorited.count %></p>
        </div>
      </div>
      <div class="blog_banner d-flex justify-content-center">
        <%= cl_image_tag @content.blog_image.key, class:"blog-img" %> 
      </div>
      <div class="blog-description">
        <h3><%= @content.description %></h3>
      </div>
    </div>
    <div class="blog-text">
      <p> <%= @content.text %> </p>
    </div>
  </div>

</div>